# Fable loop runtime state — regenerated, not source
memory/STATE.md
memory/*.log
memory/*.tsv
work-order.json
IMPLEMENTATION.md

# git worktrees the loop creates as siblings
../wt-*

# local env / secrets — NEVER commit these
.env
*.key

# OS noise
.DS_Store
